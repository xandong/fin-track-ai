import { Meta, StoryObj } from "@storybook/react"
import { UpsertTransactionDialog } from "."

const meta: Meta = {
  title: "UpsertTransactionDialog",
  component: UpsertTransactionDialog
}

export default meta

type Story = StoryObj<typeof UpsertTransactionDialog>

export const Default: Story = {}
